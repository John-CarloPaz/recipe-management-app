<nav class="bg-white shadow-md fixed top-0 w-full z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo on the left -->
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <!-- Logo Icon -->
          <div class="w-8 h-8 bg-gradient-to-br from-orange-400 to-red-500 rounded-lg flex items-center justify-center">
            <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
              <path d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z"></path>
            </svg>
          </div>
          <!-- Logo Text -->
          <span class="text-2xl font-bold text-gray-900 hover:text-orange-500 transition-colors">
            FoodieHub
          </span>
        <% end %>
      </div>
      
      <!-- Navigation Links on the right -->
      <div class="hidden md:flex items-center space-x-8">
        <%= link_to "Home", root_path, class: nav_link_class(root_path) %>
        <%= link_to "Recipes", recipes_path, class: nav_link_class(recipes_path) %>
        
        <% if current_user %>
          <%= link_to "My Recipes", recipe_list_index_path, class: nav_link_class(recipe_list_index_path) %>  
          <div class="relative">
            <button id="userMenuButton" class="flex items-center space-x-2 text-gray-700 hover:text-orange-500 focus:outline-none">
              <div class="w-8 h-8 bg-gradient-to-br from-orange-400 to-red-500 rounded-full flex items-center justify-center text-white font-semibold text-sm">
                <%= current_user.name.first.upcase %>
              </div>
              <span class="font-medium"><%= current_user.name %></span>
              <%= heroicon "chevron-down", class: "w-4 h-4", aria: { label: "dropdown-icon" } %>
            </button>
            
            <!-- Dropdown Menu -->
            <div id="userDropdown" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 py-2 z-50">
              <%= link_to new_recipe_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" do %>
                  <%= heroicon "plus", class: "w-4 h-4 inline mr-2", aria: { label: "Profile" } %>
                  New Recipe
              <% end %>
              
              <%= link_to recipe_list_index_path, class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" do %>
                <%= heroicon "document", class: "w-4 h-4 inline mr-2", aria: { label: "Profile" } %>
                My Recipes
              <% end %>
              
              <%= link_to "#", class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" do %>
                <%= heroicon "hand-thumb-up", class: "w-4 h-4 inline mr-2", aria: { label: "Profile" } %>
                Liked Recipes
              <% end %>
              
              <%= link_to "#", class: "block px-4 py-2 text-gray-700 hover:bg-gray-100 transition-colors" do %>
                <%= heroicon "adjustments-horizontal", class: "w-4 h-4 inline mr-2", aria: { label: "Settings" } %>
                Settings
              <% end %>
              
              <hr class="my-2 border-gray-200">
              
              <%= button_to logout_path, method: :delete, 
                  class: "w-full text-left px-4 py-2 text-red-600 hover:bg-red-50 transition-colors border-0 bg-transparent",
                  form: { style: "display: block;" } do %>
                <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                </svg>
                Sign Out
              <% end %>
            </div>
          </div>
        <% else %>
          <div class="flex items-center space-x-4">
            <%= link_to "Sign In", new_session_path, class: "text-gray-700 hover:text-orange-500 font-medium transition-colors" %>
            <%= link_to "Sign Up", new_user_path, class: "bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded-lg transition-colors font-medium" %>
          </div>
        <% end %>
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden">
        <button type="button" class="mobile-menu-button text-gray-500 hover:text-gray-600 focus:outline-none focus:text-gray-600" aria-label="toggle menu">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Menu -->
    <div class="mobile-menu hidden md:hidden pb-4">
      <div class="flex flex-col space-y-2">
        <%= link_to "Home", root_path, class: "text-gray-600 hover:text-orange-500 py-2 px-4 rounded transition-colors" %>
        <%= link_to "Recipes", recipes_path, class: "text-gray-600 hover:text-orange-500 py-2 px-4 rounded transition-colors" %>
        
        <% if current_user %>
          <%= link_to "My Recipes", recipe_list_index_path, class: "text-gray-600 hover:text-orange-500 py-2 px-4 rounded transition-colors" %>
          <%= link_to "Add Recipe", new_recipe_path, class: "bg-orange-500 hover:bg-orange-600 text-white py-2 px-4 rounded-lg transition-colors font-medium mt-2" %>
          
          <!-- Mobile User Info -->
          <div class="border-t border-gray-200 pt-4 mt-4">
            <div class="flex items-center px-4 py-2">
              <div class="w-10 h-10 bg-gradient-to-br from-orange-400 to-red-500 rounded-full flex items-center justify-center text-white font-semibold">
                <%= current_user.name.first.upcase %>
              </div>
              <div class="ml-3">
                <p class="text-gray-800 font-medium"><%= current_user.name %></p>
                <p class="text-gray-500 text-sm"><%= current_user.email %></p>
              </div>
            </div>
            <%= link_to "Profile", "#", class: "text-gray-600 hover:text-orange-500 py-2 px-4 rounded transition-colors" %>
            <%= link_to "Settings", "#", class: "text-gray-600 hover:text-orange-500 py-2 px-4 rounded transition-colors" %>
            
            <!-- Fixed mobile logout using button_to -->
            <%= button_to "Sign Out", logout_path, method: :delete, 
                class: "text-red-600 hover:text-red-700 py-2 px-4 rounded transition-colors w-full text-left border-0 bg-transparent" %>
          </div>
        <% else %>
          <%= link_to "Sign In", new_session_path, class: "text-gray-600 hover:text-orange-500 py-2 px-4 rounded transition-colors" %>
          <%= link_to "Sign Up", new_user_path, class: "bg-orange-500 hover:bg-orange-600 text-white py-2 px-4 rounded-lg transition-colors font-medium mt-2" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

<!-- JavaScript for Mobile Menu and User Dropdown -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Mobile Menu Toggle
    const mobileMenuButton = document.querySelector('.mobile-menu-button');
    const mobileMenu = document.querySelector('.mobile-menu');

    if (mobileMenuButton && mobileMenu) {
      mobileMenuButton.addEventListener('click', function() {
        mobileMenu.classList.toggle('hidden');
      });
    }

    // User Dropdown Toggle
    const userMenuButton = document.getElementById('userMenuButton');
    const userDropdown = document.getElementById('userDropdown');

    if (userMenuButton && userDropdown) {
      userMenuButton.addEventListener('click', function(e) {
        e.stopPropagation();
        userDropdown.classList.toggle('hidden');
      });

      // Close dropdown when clicking outside
      document.addEventListener('click', function(e) {
        if (!userMenuButton.contains(e.target) && !userDropdown.contains(e.target)) {
          userDropdown.classList.add('hidden');
        }
      });

      // Close dropdown when pressing Escape
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') {
          userDropdown.classList.add('hidden');
        }
      });
    }
  });
</script>